import * as React from "react";
import { Typography } from "@material-ui/core";

export const Dashboard: React.FC = () => {
  return (
    <>
      <Typography variant="h4">Proxy-able Containers</Typography>
      <Typography variant="h4">All Containers</Typography>
    </>
  );
};
